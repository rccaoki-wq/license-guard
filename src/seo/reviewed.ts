/**
 * ライセンス判定データを最後に見直した日。
 *
 * **リクエスト時刻から作らない。** `new Date()` を使うと毎日「今日更新した」と
 * 名乗ることになり、実際には何も見直していない。検索側は更新頻度と実際の
 * 変化量を突き合わせられるので、動かない内容に新しい日付を付け続けると
 * 鮮度シグナルとして効かなくなるどころか信用を落とす。
 *
 * ライセンスの解釈は年単位でしか動かない。**動いていないなら日付も動かさない。**
 * これは正直な表示であって、新しく見せるための細工ではない。
 *
 * 更新するとき: `src/policy/` の判定か `src/seo/catalog.ts` の内容を
 * 実際に見直したときだけ、その日付に書き換える。
 */
export const LAST_REVIEWED = '2026-08-24';

/** schema.org / <time> 用。日付だけで時刻は持たない */
export const LAST_REVIEWED_ISO = LAST_REVIEWED;
